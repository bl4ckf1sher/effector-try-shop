import { Layout } from "antd";

const { Footer } = Layout;
const AppFooter: React.FC = () => (
  <Footer style={{ textAlign: "center" }}>
    Micro Shop ©2024 Created by blackfisher
  </Footer>
);

export default AppFooter;
